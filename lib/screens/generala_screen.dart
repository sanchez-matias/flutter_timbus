import 'package:flutter/material.dart';

class GeneralaScreen extends StatelessWidget {
  const GeneralaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Generala Screen'),
      ),
    );
  }
}
