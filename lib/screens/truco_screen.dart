import 'package:flutter/material.dart';

class TrucoScreen extends StatelessWidget {
  const TrucoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Truco Screen'),
      ),
    );
  }
}
