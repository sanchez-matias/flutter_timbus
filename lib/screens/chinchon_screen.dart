import 'package:flutter/material.dart';

class ChinchonScreen extends StatelessWidget {
  const ChinchonScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Chinchon Screen'),
      ),
    );
  }
}
